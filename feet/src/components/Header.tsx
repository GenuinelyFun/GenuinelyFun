import React from "react";
import "./Header.css";
import Menu from "./Menu";

const Header = () => {
    return (
        <header>
            <Menu />
        </header>
    );
};

export default Header;